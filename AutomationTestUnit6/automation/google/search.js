// const { By } = require('selenium-wbdriver';

// const search = async (driver, searchTerm) => {
//     await driver.findElement(By.name("q")).sendKeys(`${searchTerm}\n`);
    
//     let resultsText = await driver.findElement
// }